import Modal from "./Modal";


const LowBalWarning = ({isOpen , setIsOpen}) => {
    return (
        <Modal isOpen={isOpen} setIsOpen={setIsOpen}>
            
        </Modal>
    );
};

export default LowBalWarning;